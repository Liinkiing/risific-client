mutation AddUserFavorite ($input: AddUserFavoriteInput!) {
  addUserFavorite(input: $input) {
    userFavorite {
      risific {
        id
        __typename
        viewerHasFavorited
      }
    }
  }
}
