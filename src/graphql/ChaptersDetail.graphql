query ChaptersDetail($slug: String!, $chapter: String!) {
    chapters(risific_slug: $slug, position: $chapter) {
        edges {
            node {
                body
            }
        }
    }
}